/*
Objeto é uma estrutura de dados que armazena uma lista de valores no formato chave: valor, sendo as propriedades separadas por vvírgula
*/

let obj1 = {
  nome: "Matheus",
  idade: 15
};

// Obtemos os valores das propriedades de um objeto pela chave dela, como no exemplo abaixo.
console.log(obj1.nome, typeof obj1);