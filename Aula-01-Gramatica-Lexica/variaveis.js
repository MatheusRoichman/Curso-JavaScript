// Inicialmente, variáveis são espaços que definimos para armazenar um dado (ex: um texto, um número...).

var numero = 2;

// Obs: a função console.log() exibe uma mensagem no console. Não é visível para o usuário, logo deve ser usado apenas em testes no desenvolvimento.
console.log(numero);